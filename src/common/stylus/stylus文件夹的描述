mixin.styl

  //此文件中定义了一个css预处理器函数,用来处理1像素问题,用的时候在样式中 函数名(参数)

   //代码中的 :after是伪元素,用来在元素后添加一个新元素




base.styl

  //此文件是用来兼容移动端(也就是手机),因为pc端显示是一像素,在移动端就是2像素了,
  //所以这里进行了缩放,不同的手机类型进行不同比例的缩放
  //使用:在需要用到这个东西的元素上添加类名  .border-1px
